#include "main.h"

/**
 * _abs -  function that computes the absolute value of an integer.
 * @n: value of the number to print
 * Return:0
 */

int _abs(int n)
{
	if (n > 0)
	{
		return (n);
	}
	else
	{
		return (-n);
	}
	return (0);
}
